---
---
<style>
  .siteFooter { margin: 3.5rem auto 2.5rem; padding: 0 0.75rem; text-align: center; color: var(--pale-gray); }
  .inner { max-width: 960px; margin: 0 auto; }
  .line { margin: 0.2rem 0; font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; text-shadow: 0 0 8px rgba(84,72,200,0.35); }
  .terminal { opacity: 0.95; }
  .cursor { display: inline-block; width: .8ch; color: #5448c8; animation: blink 1s steps(1, end) infinite; }
  @keyframes blink { 50% { opacity: 0; } }
  @media (prefers-reduced-motion: reduce) { .cursor { animation: none; } }
</style>

<footer class="siteFooter" aria-label="Site footer">
  <div class="inner">
    <p class="line">Crafted somewhere in the cosmos by <strong>Patrick Gatewood</strong></p>
    <p class="line terminal">[END TRANSMISSION]<span class="cursor">█</span></p>
  </div>
 </footer>


